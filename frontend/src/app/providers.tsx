// Reserved for cross-cutting providers (theme, error boundary, etc.)
// as the app grows — main.tsx composes Redux/React Query/Router directly
// for now since there are only three providers.
export {};
